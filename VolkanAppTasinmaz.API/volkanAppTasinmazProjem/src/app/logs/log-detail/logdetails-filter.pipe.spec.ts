import { LogdetailsFilterPipe } from './logdetails-filter.pipe';

describe('LogdetailsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LogdetailsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
