import { TasinmazFilterPipe } from './tasinmaz-filter.pipe';

describe('TasinmazFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TasinmazFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
